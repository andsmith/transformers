/**
 * App version, shown in the title bar and browser tab.
 *
 * Bump the patch number by one on every push that deploys an update
 * (0.0.1 -> 0.0.2 -> ...). Single source of truth — used by both the in-app
 * title and `document.title`.
 */
export const VERSION = "0.0.40";
